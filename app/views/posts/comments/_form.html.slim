= simple_form_for comment, url:, html: { class: custom_class } do |f|
  = f.input :parent_id, as: :hidden
  = f.input :content, label: false

  = f.button :submit, t('post.comment.new', scope: 'simple_form.submit_buttons'), class: 'btn-primary'
